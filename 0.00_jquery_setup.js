/* 
.ready() is an "event" that's "listening" to the document. 
Once the document is done loading, the code inside .ready() is run. 
*/
$(document).ready(function(){
	alert("Hey, the jQuery is working.");
});

// An anonymous function doesn't have a name associated w/it.